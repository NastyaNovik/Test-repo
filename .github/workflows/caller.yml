name: Caller Workflow

on:
  workflow_dispatch:

jobs:
  call-example:
    uses: ./.github/workflows/receiver.yml
    with:
      message: "Hello from caller 👋"
      branch: ${{ github.ref_name }}
